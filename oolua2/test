import "This", "That" from "path"
-- local import = "test" -- Raises an error: reserved keyword

class MyClass(InheritsFrom, That)
    static my_static_field = "hello" .. string.split("world", "o")[1]
    print("test")

    static function testFunction(args, ...)
        print("test")
    end

    local function myPrivateFunc()

    end

    function init(self, ...)

    end
-- body

end
